package com.bitproject.techbeats.ram.repositary;
import com.bitproject.techbeats.ram.model.RamLatency;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RamLatencyRepository extends JpaRepository<RamLatency,Integer> {
}
